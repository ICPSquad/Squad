export type RegistrationObject = {
  email?: string;
  discord?: string;
  twitter?: string;
  wallet: string;
};

export type FormObject = {
  email?: string;
  discord?: string;
  twitter?: string;
};
