export type Color = [number, number, number, number];
export type AvatarColors = {
  background: Color;
  skin: Color;
  hairs: Color;
  eyes: Color;
  eyebrows: Color;
  eyeliner: Color;
  clothes: Color;
};
